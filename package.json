{
  "name": "readify-broadsheet",
  "version": "1.0.0",
  "description": "Readify — Broadsheet Gazette & Letterpress Print Shop eReader & Audiobook App for Android APK & Web",
  "main": "index.html",
  "scripts": {
    "build": "powershell -Command \"New-Item -ItemType Directory -Force -Path www; Copy-Item -Recurse -Force index.html, css, js, assets www/\"",
    "start": "npx serve www",
    "cap:add": "npm run build && npx cap add android",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "keywords": [
    "pdf-reader",
    "epub-reader",
    "broadsheet",
    "letterpress",
    "text-to-speech",
    "audiobook",
    "apk",
    "android"
  ],
  "author": "Readify Gazette Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
